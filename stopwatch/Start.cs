using System;

namespace OOP_With_CSharp
{
    public class Starter
    {
        public DateTime Start(DateTime start)
        {
            start = DateTime.Now;
            return start;
        }
    }
}